# Helion (Unity Edition)

A fast paced Doom-like game built with Unity.

## Development

To avoid any updates with the command line arguments game object, run this from the command line on checking out from the top level directory:

```bash
git update-index --assume-unchanged Helion/Assets/Scripts/Helion/Unity/DebugCommandLineArgs.cs
```
